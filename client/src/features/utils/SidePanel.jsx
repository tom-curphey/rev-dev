import React from 'react';

// You created this so you had a component to
// style the background of the side panel
// However a class can do this so there isn't much point..

export default function SidePanel() {
  return <section className="side_panel" />;
}
